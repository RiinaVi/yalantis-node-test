import path from 'path';

export const PUBLIC_DIRECTORY = path.join(__dirname, '../../', '/public');
export const IMAGES_DIRECTORY = path.join(PUBLIC_DIRECTORY, 'images');
export const DATA_FILE = `${PUBLIC_DIRECTORY}/data.json`;
